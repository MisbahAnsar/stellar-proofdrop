export type TaskMetadata = {
  taskId: string;
  title: string;
  description: string;
  deadline?: string;
  creator: string;
  rewardStroops: string;
  transactionHash: string;
  createdAt: string;
};
